<nav class="top-nav blue darken-2"> 
  <div class="container nav-wrapper blue darken-2">
    <a href="/" class="brand-logo">ClearVoltex</a>
    <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menus</i></a>
    <ul class="right hide-on-med-and-down">
      <% if logged_in? %>
        <li>Logged in as <%= current_user.display_name %></li>
        <% if current_user.role == "admin" %>
          <li><%= link_to "Admin Panel", '/admin' %></li>
        <% else %>
          <li><%= link_to "All Songs", user_path(current_user.id) %></li>
        <% end %>
        <li><%= link_to "Settings", edit_user_path(current_user.id) %></li>
        <li><%= link_to "Log out", logout_path, method: "delete"%></li>
      <% else %>
        <li><%= link_to "Log in", login_path %></li>
        <li><%= link_to "Sign Up", signup_path %></li>
      <% end %>
    </ul>
    <ul class="side-nav" id="mobile-demo">
      <% if logged_in? %>
        <li>Logged in as <%= current_user.display_name %></li>
        <% if current_user.role == "admin" %>
          <li><%= link_to "Admin Panel", '/admin' %></li>
        <% else %>
          <li><%= link_to "All Songs", user_path(current_user.id) %></li>
        <% end %>
        <li><%= link_to "Settings", edit_user_path(current_user.id) %></li>
        <li><%= link_to "Log out", logout_path, method: "delete"%></li>
      <% else %>
        <li><%= link_to "Log in", login_path %></li>
        <li><%= link_to "Sign Up", signup_path %></li>
      <% end %>
    </ul>
  </div>
</nav>