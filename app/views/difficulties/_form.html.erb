<%= form_for(difficulty) do |f| %>
  <% if difficulty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(difficulty.errors.count, "error") %> prohibited this difficulty from being saved:</h2>

      <ul>
      <% difficulty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :song_id %>
    <%= f.number_field :song_id %>
  </div>

  <div class="field">
    <%= f.label :difficulty_name %>
    <%= f.text_field :difficulty_name %>
  </div>

  <div class="field">
    <%= f.label :level %>
    <%= f.number_field :level %>
  </div>

  <div class="field">
    <%= f.label :photo %>
    <%= f.text_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
