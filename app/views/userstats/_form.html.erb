<%= form_for(userstat) do |f| %>
  <% if userstat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userstat.errors.count, "error") %> prohibited this userstat from being saved:</h2>

      <ul>
      <% userstat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :difficulty_id %>
    <%= f.number_field :difficulty_id %>
  </div>

  <div class="field">
    <%= f.label :clear %>
    <%= f.text_field :clear %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
